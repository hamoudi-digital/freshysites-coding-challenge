The code for the challenges are separated in their own folders based off of challenge number.

The scenario question is answered in its own folder as a txt file in "question > answer.txt".

The following are in the root directory as a necessity for challenge 4: 
node_modules, package-lock.json, and package.json